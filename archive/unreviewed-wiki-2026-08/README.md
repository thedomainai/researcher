# Unreviewed wiki articles (quarantined)

Twenty articles generated by an incremental `compile_wiki.py` run on 2026-08-18.
That run used a 20-concept `concepts.json` that replaced the real 713-concept file,
and it also rewrote the internal links of 695 existing articles. The articles here
were never listed in `wiki/index.md` or `wiki/_meta/concepts.json`, and their source
paths are not resolvable (some say "パス未確認").

They are kept only so the generated text is not lost. They are not part of the
knowledge base. Do not cite them as evidence, and do not move them back into
`wiki/concepts/` without reviewing each article against its sources.
