# Verification Scripts

`verify_pipeline.sh` is the default verification entrypoint for the researcher project.

Checks:

- required project files exist
- Python pipeline modules compile successfully
- key config and wiki entrypoints are present
